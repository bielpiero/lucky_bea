#include "outputVariable.h"

namespace fuzzy{
	outputVariable::outputVariable(const std::string name, float minRange, float maxRange) : variable(name, minRange, maxRange){
            
        }
	outputVariable::~outputVariable(){
            
        }

}